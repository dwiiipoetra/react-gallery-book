import React, { useState } from "react";

const BookCard = ({
  smallThumbnail,
  title,
  publisher,
  authors,
  previewLink,
}) => {
  return (
    <div>
      hehe
    </div>
  );
};

export default BookCard;
